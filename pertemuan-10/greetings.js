// const user = {
//   name: "Skilvul",
// };

let sayHello = (user) => {
  alert(`hello ${user}`);

  // template literal string
  alert("hello " + user);
  alert(`hello ${user}`);
};

export default sayHello;

// const sayMyData = (name, age) => {
//   alert(`hello my name ${name}, my age is ${age}`);
// };

// export { sayHello, sayMyData, user };
